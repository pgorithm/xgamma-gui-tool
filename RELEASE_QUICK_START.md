# Быстрый старт: Создание релиза с .deb пакетом

## 🚀 Простая инструкция (5 минут)

### Шаг 1: Обновите информацию о себе (только первый раз)

Откройте и отредактируйте два файла:

**`debian/control`** — замените:
- `Your Name` → ваше имя
- `your.email@example.com` → ваш email

**`debian/changelog`** — замените:
- `Your Name` → ваше имя
- `your.email@example.com` → ваш email
- Дату на текущую

### Шаг 2: Добавьте файлы в Git

```bash
git add .github/workflows/build-deb.yml
git add debian/
git commit -m "Добавлена автоматическая сборка .deb пакетов"
git push origin main
```

### Шаг 3: Создайте релиз на GitHub

1. Откройте ваш репозиторий на GitHub
2. Нажмите **"Releases"** (справа в меню)
3. Нажмите **"Create a new release"**
4. В поле **"Tag version"** введите: `v1.0.0`
5. Выберите **"Create tag: v1.0.0 on publish"**
6. Заполните название: `xgamma GUI Tool v1.0.0`
7. Нажмите **"Publish release"**

### Шаг 4: Подождите сборку (2-5 минут)

1. Перейдите на вкладку **"Actions"**
2. Дождитесь завершения процесса "Build DEB package" (зеленая галочка ✅)
3. Вернитесь на страницу **"Releases"**
4. В разделе **"Assets"** будет готовый файл `xgamma-gui-tool_1.0.0-1_all.deb`

## 📦 Что дальше?

Для следующих релизов просто:
1. Обновите код
2. Создайте новый тег (например, `v1.1.0`)
3. GitHub автоматически соберет новый пакет!

## ❓ Проблемы?

Смотрите подробное руководство: [`docs/release_guide.md`](docs/release_guide.md)

