# Progress

- **TASK-001 (2026-04-04):** Ошибки `applyGamma` (отложенное применение и Reset) показываются в статусной строке; полный вывод последнего apply хранится в `GammaCore.lastApplyRawOutput` / `getLastApplyRawOutput()`. Ручная проверка сбоя xgamma — под Linux/X11.
- **TASK-002 (2026-04-04):** `ConfigManager.saveToAutostart` / `removeFromAutostart` возвращают результат с краткой причиной для статусной строки; полный traceback и путь — в лог (`logging`). Reset удаляет только `xgamma_gui_tool.desktop` (PRD 3.6). Ручная проверка отказа записи в autostart — под Linux/X11.
- **TASK-003 (2026-04-04):** После инициализации в статусе больше не показывается сырой многострочный вывод xgamma; для успешного чтения — «Ready», для xrandr/сбоя — короткие сообщения, диагностика в лог. `py -m compileall` ок; полные шаги 2–3 — вручную под X11.
- **TASK-004 (2026-04-04):** До `InitializationWorker` слайдеры/поля ввода/Reset/Apply отключены, в полях placeholder «…», превью — «Reading display gamma…», статус — «Loading initial settings…»; после `finished` — реальные значения и включение контролов. `py -m compileall` ок; шаги 2–3 под X11.
- **TASK-005 (2026-04-04):** Для строки All — `setToolTip`/`setStatusTip` на подписи, слайдере и поле ввода: среднее R/G/B при расхождении каналов и выравнивание всех трёх при движении All; комментарий в `_onSliderChanged` согласован. `py -m compileall` ок; шаги 2–3 (разные R/G/B, движение All) — вручную под X11.
- **TASK-006 (2026-04-04):** Жёлтый баннер под верхней панелью с полным текстом VM/HDR до «Dismiss»; иконка и tooltip сохранены; статус после инициализации дополняется короткой отсылкой к заметке. `py -m compileall` ок; видимость/сворачивание и гамма — вручную под X11 (или заглушка предупреждений).
- **TASK-007 (2026-04-04):** Превью эталона без `setScaledContents`: полный pixmap хранится, в лейбл кладётся `scaled(..., KeepAspectRatio, SmoothTransformation)`; ресайз лейбла дебаунсится 50 мс (отдельный QTimer). `py -m compileall` ок; пропорции и отзывчивость слайдеров — вручную под X11.
- **TASK-008 (2026-04-04):** `QApplication.focusChanged` синхронизирует `activeChannel` с фокусом; подписи каналов — Tab/ClickFocus, tooltip про ←/→ и Shift; жирная подпись для активного ряда; фильтр стрелок только при `activeChannel ==` виджету с фокусом (клик по превью сбрасывает канал — нативные стрелки у слайдера). `py -m compileall` ок; шаги 2–3 — вручную под X11.
- **TASK-009 (2026-04-04):** `AboutDialog` по клику «About» в Settings; версия из `src/version_info.py` (`dev` из исходников). В `build-deb.yml` после обновления changelog подставляется `__version__` из тега релиза. `py -m compileall` ок; открытие диалога и версия в DEB — вручную / на CI.
- **TASK-012 (2026-04-04):** Проверки VM/HDR вынесены в `src/environment_checks.py` (`EnvironmentProbe`, `collect_environment_warnings`); `InitializationWorker` вызывает один вход, без вложенного `DummyMainWindow`. `py -m compileall` ок; баннер/иконка под X11 — вручную.
- **TASK-010 (2026-04-04):** `install_translators()` в `src/main.py` после `QApplication`; `src/i18n.py` — загрузка `translations/xgamma_gui_ru.qm` при наличии иначе `EmbeddedRussianTranslator` при русской локали/LANG; строки главного окна, диалогов и предупреждений окружения через `tr()` / `QCoreApplication.translate`. Инструкции: `README.md`, `src/translations/README.txt`. `py -m compileall` ок; `LANG=ru_RU.UTF-8` и GUI — вручную под Linux/X11.
- **TASK-011 (2026-04-04):** Раздел «Calibration tips» в `AboutDialog`; подсказки/status tip для R/G/B и All — нейтральность 1.0, типичное использование каналов и клавиатура (←/→, Shift); диапазоны слайдеров не менялись. `py -m compileall` ок; читаемость подсказок и полный диапазон — вручную под X11 / `LANG=ru`.
- **TASK-013 (2026-04-04):** Удалены неиспользуемые виджеты `SettingsButton`…`SettingsSlider` из `gui.py`; `SettingsDialog` по-прежнему на `QPushButton` + `AboutDialog`. Импорт `QCheckBox` убран. `py -m compileall` ок; smoke GUI — вручную под X11.
- **TASK-014 (2026-04-04):** В `gui.py` выровнены docstring у `_updateReferenceImage` / `_applyPendingGamma`, убраны пробелы на пустых строках, комментарии в `_onResetClicked` приведены к одному стилю (в т.ч. длинная строка про `gammaApplyTimer`). Логика не менялась. `py -m compileall` ок; краткий GUI smoke — вручную под X11.
